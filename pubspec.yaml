name: drift_stream_timer_issue
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.21.0
  drift_flutter: ^0.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  drift_dev: ^2.21.2
  build_runner: ^2.4.13
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
